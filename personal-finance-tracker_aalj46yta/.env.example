# Session secret for authentication cookies
# Generate a secure random string: openssl rand -base64 32
SESSION_SECRET=your_session_secret_here

# Set to "true" to seed the database with example data on server start
SEED_DATABASE=false

# Timezone for the application (optional, defaults to UTC)
APP_TIMEZONE=UTC