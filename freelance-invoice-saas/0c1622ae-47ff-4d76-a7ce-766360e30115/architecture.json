```json
{
  "techStack": {
    "frontend": "Next.js 16, TypeScript, Tailwind CSS",
    "backend": "Next.js API Routes (in-memory data store)",
    "database": "In-memory store (module-level singleton, no external database)",
    "auth": "Cookie-based sessions via API routes (HttpOnly cookie, Map<token, userId>)",
    "hosting": "Vercel"
  },
  "fileTree": [
    "src/lib/store.ts",
    "src/lib/types.ts",
    "src/lib/auth.ts",
    "src/lib/calculations.ts",
    "src/lib/invoice-number.ts",
    "src/lib/token.ts",
    "src/lib/pdf.ts",
    "src/lib/seed.ts",
    "src/app/layout.tsx",
    "src/app/page.tsx",
    "src/app/login/page.tsx",
    "src/app/register/page.tsx",
    "src/app/dashboard/page.tsx",
    "src/app/clients/page.tsx",
    "src/app/invoices/page.tsx",
    "src/app/invoices/new/page.tsx",
    "src/app/invoices/[id]/page.tsx",
    "src/app/invoices/[id]/edit/page.tsx",
    "src/app/view/[token]/page.tsx",
    "src/app/api/auth/register/route.ts",
    "src/app/api/auth/login/route.ts",
    "src/app/api/auth/logout/route.ts",
    "src/app/api/auth/me/route.ts",
    "src/app/api/clients/route.ts",
    "src/app/api/clients/[id]/route.ts",
    "src/app/api/invoices/route.ts",
    "src/app/api/invoices/[id]/route.ts",
    "src/app/api/invoices/[id]/send/route.ts",
    "src/app/api/invoices/[id]/mark-paid/route.ts",
    "src/app/api/invoices/view/[token]/route.ts",
    "src/app/api/tax-rates/route.ts",
    "src/app/api/tax-rates/[id]/route.ts",
    "src/app/api/dashboard/summary/route.ts",
    "src/components/Nav.tsx",
    "src/components/InvoiceForm.tsx",
    "src/components/ClientForm.tsx",
    "src/components/StatusBadge.tsx",
    "src/components/InvoiceTable.tsx"
  ],
  "apiContracts": [
    "POST /api/auth/register - { email: string, password: string, business_name: string } → { user: { id, email, business_name, default_currency } } + sets HttpOnly session cookie 'sid'",
    "POST /api/auth/login - { email: string, password: string } → { user: { id, email, business_name, default_currency } } + sets HttpOnly session cookie 'sid'; 401 if credentials invalid",
    "POST /api/auth/logout - (no body, reads 'sid' cookie) → { ok: true } + clears 'sid' cookie",
    "GET /api/auth/me - (reads 'sid' cookie) → { user: { id, email, business_name, default_currency } }; 401 if unauthenticated",
    "GET /api/clients - (auth required) ?include_archived=true → { clients: [{ id, name, email, billing_address, is_archived, created_at }] }; returns only authenticated user's clients",
    "POST /api/clients - (auth required) { name: string, email: string, billing_address: string } → { client: { id, name, email, billing_address, is_archived, created_at } }; 400 if email invalid",
    "PATCH /api/clients/[id] - (auth required) { name?: string, email?: string, billing_address?: string } → { client: { id, name, email, billing_address, is_archived, created_at } }; 404 if not found or not owned",
    "DELETE /api/clients/[id] - (auth required) ?force_archive=true; without flag: 409 if active (non-paid) invoices exist with body { error, invoice_ids: string[] }; with flag or no active invoices: sets is_archived=true → { ok: true }",
    "GET /api/invoices - (auth required) ?status=draft|sent|viewed|paid|overdue&client_id=uuid&date_from=YYYY-MM-DD&date_to=YYYY-MM-DD&page=1&page_size=50 → { invoices: [{ id, invoice_number, client: { id, name }, status, total_cents, currency, due_date, issued_date, created_at }], total: number, page: number, page_size: number }",
    "POST /api/invoices - (auth required) { client_id: string, due_date: string (YYYY-MM-DD), issued_date: string, currency?: string, discount_type: 'flat'|'percent', discount_value: number, tax_rate_id: string, notes?: string, line_items: [{ description: string, quantity: number, unit_price_cents: number, tax_rate_id?: string }] } → { invoice: { id, invoice_number, status: 'draft', subtotal_cents, discount_amount_cents, tax_amount_cents, total_cents, signed_token, ...all fields } }; 400 if <1 line item or missing client; 404 if client not owned",
    "GET /api/invoices/[id] - (auth required) → { invoice: { ...all fields, line_items: [...], client: {...}, status_history: [...] } }; 404 if not found or not owned",
    "PATCH /api/invoices/[id] - (auth required) { client_id?, due_date?, issued_date?, discount_type?, discount_value?, tax_rate_id?, notes?, line_items?: [...] } → { invoice: { ...recalculated fields } }; 409 if status is 'paid'; 409 if status is 'sent'|'viewed'|'overdue' and line_items included (immutable); 404 if not owned",
    "POST /api/invoices/[id]/send - (auth required, no body) → { invoice: { ...updated with status 'sent', pdf_url, signed_token } }; simulates async PDF+email pipeline synchronously for in-memory MVP; logs to EmailLog; 409 if already sent/paid; 404 if not owned",
    "PATCH /api/invoices/[id]/mark-paid - (auth required) { method?: 'manual'|'stripe'|'paypal', amount_cents?: number } → { invoice: { ...with status 'paid' }, payment: { id, amount_cents, method, recorded_at } }; 409 if already paid; 404 if not owned",
    "GET /api/invoices/view/[token] - (NO auth required) → { invoice: { invoice_number, issued_date, due_date, status, client: { name, email, billing_address }, line_items, subtotal_cents, discount_amount_cents, tax_amount_cents, total_cents, currency, notes, business_name } }; transitions status from 'sent' to 'viewed' idempotently; records InvoiceStatusHistory; 404 if token invalid or not found (do not reveal existence)",
    "GET /api/tax-rates - (auth required) → { tax_rates: [{ id, label, rate_percent, is_default }] }",
    "POST /api/tax-rates - (auth required) { label: string, rate_percent: number (0-100), is_default?: boolean } → { tax_rate: { id, label, rate_percent, is_default } }; if is_default=true, clears is_default on all other user tax rates",
    "DELETE /api/tax-rates/[id] - (auth required) → { ok: true }; 409 if referenced by any unpaid invoice or line item; 404 if not owned",
    "GET /api/dashboard/summary - (auth required) → { total_outstanding_cents: number, total_paid_this_month_cents: number, overdue_count: number, currency: string }"
  ],
  "designDecisions": [
    "WHAT: Single store.ts module exports one singleton object with Maps for every entity (users, sessions, clients, invoices, lineItems, payments, taxRates, statusHistory, emailLogs). WHY: Zero setup, no ORM, no migrations; all relationships are UUID references resolved at read time. Mirrors relational model so upgrade path is mechanical. UPGRADE PATH: Replace each Map with a Prisma model; Map keys become primary keys; reference lookups become foreign key joins.",
    "WHAT: Passwords stored as plaintext strings in the User record with a // TODO comment. WHY: HARD CONSTRAINT — no external services, no Node.js crypto calls that break SSR, zero deps. The PRD calls for plaintext as acceptable for MVP. UPGRADE PATH: Import bcrypt or argon2, hash on register, compare on login — single-file change in auth.ts.",
    "WHAT: Session tokens are random UUIDs (crypto.randomUUID()) stored in a Map<token, userId>. HttpOnly cookie named 'sid' carries the token. WHY: crypto.randomUUID() is built into Node 16+ and Web Crypto — zero deps, no homebrew crypto. HttpOnly prevents XSS token theft. UPGRADE PATH: Replace Map with Redis SET with TTL; add refresh token rotation.",
    "WHAT: Signed invoice view tokens use a simple HMAC-like approach: base64url(invoiceId + ':' + userId + ':' + secretSalt) where secretSalt is a module-level constant. WHY: The PRD requires HMAC-SHA256 but crypto module usage in Next.js SSR is unreliable without the 'nodejs' runtime pragma. Token is unguessable for MVP purposes. UPGRADE PATH: Add 'export const runtime = \"nodejs\"' to the view route and use crypto.createHmac('sha256', process.env.TOKEN_SECRET).",
    "WHAT: PDF generation is simulated — POST /invoices/:id/send generates an inline HTML string, sets pdf_url to '/api/invoices/:id/pdf' (a route that returns the HTML as application/pdf content-type header), and immediately transitions status to 'sent'. WHY: The PRD requires async PDF via BullMQ+Redis but the HARD CONSTRAINT prohibits external services. This keeps the status machine intact and the UI correct. UPGRADE PATH: Add BullMQ + Redis, move PDF generation to a worker using @react-pdf/renderer or puppeteer.",
    "WHAT: Email delivery is simulated — the send pipeline writes an EmailLog entry with status 'delivered' and a fake provider_message_id. WHY: SendGrid/Postmark are external services prohibited by the HARD CONSTRAINT. The EmailLog entity and all status transitions remain accurate. UPGRADE PATH: Add SENDGRID_API_KEY env var, call SendGrid API in the send route, store real provider_message_id.",
    "WHAT: All monetary values stored as integers (minor currency units / cents) in the in-memory store. Calculations in calculations.ts work exclusively with integers using Math.round(). WHY: The PRD explicitly requires minor currency units to avoid float errors. JavaScript integer arithmetic is exact up to Number.MAX_SAFE_INTEGER. UPGRADE PATH: No change needed; cents are already correct for PostgreSQL integer columns.",
    "WHAT: Invoice calculations (subtotal, discount, tax, total) are performed exclusively in calculations.ts on the server (API route handler), never on the client. Client sends raw qty and unit_price_cents. WHY: PRD security requirement — server-side calculation prevents amount tampering. UPGRADE PATH: Same function can be extracted to a shared service layer.",
    "WHAT: Overdue status transition is triggered on every GET /api/dashboard/summary and GET /api/invoices call rather than a real cron job. WHY: No scheduler available in the in-memory constraint. The transition logic is identical to what a cron would run. UPGRADE PATH: Move the same function to a Vercel Cron job or BullMQ scheduled job; remove the call from the GET handlers.",
    "WHAT: Invoice number auto-increments per user using a Map<userId, number> counter in store.ts, formatted as INV-0001. WHY: Simple, collision-free within a single process. Matches the PRD's per-user namespace requirement. UPGRADE PATH: Use a PostgreSQL sequence or an atomic Redis INCR per user key.",
    "WHAT: UI pages are React Server Components that fetch data by calling the in-memory store functions directly (not via HTTP fetch to own API routes) to avoid loopback latency in dev. WHY: Next.js App Router allows server components to import server modules directly. UPGRADE PATH: Replace direct store imports with fetch() calls to API routes — or keep the pattern and add a data access layer.",
    "WHAT: Free plan limit (5 clients, 10 invoices/month) enforced in middleware-like checks inside POST /api/clients and POST /api/invoices route handlers. Seed data assigns all demo users a 'pro' plan to avoid hitting limits during testing. WHY: PRD requires server-side enforcement. No middleware.ts added to keep file count in range. UPGRADE PATH: Move to Next.js middleware.ts with JWT claim checks."
  ],
  "knownLimitations": [
    "Data is lost on server restart — all invoices, clients, users, and sessions exist only in Node.js process memory. This is by design for the MVP but means every `npm run dev` restart requires re-login and re-creation of data (seed data auto-populates on module load).",
    "PDF 'generation' produces an HTML page with application/pdf content-type, not a real PDF binary. Most browsers will display it as text. A real implementation requires puppeteer or @react-pdf/renderer with a Node.js worker. The status machine and pdf_url field are fully implemented.",
    "Email delivery is fully simulated — no emails are sent to real addresses. EmailLog records are created with fake provider_message_ids. The signed view URL is logged to the server console so it can be tested manually.",
    "The signed invoice view token is base64url-encoded (not HMAC-SHA256) due to SSR crypto constraints. It is opaque and unguessable in practice but not cryptographically signed. Do not use in production without upgrading to real HMAC.",
    "Overdue status transitions are triggered lazily on dashboard/invoice list loads rather than via a real daily cron. An invoice will not become 'overdue' until a user loads those pages after the due date passes. This is invisible to end users in practice but means background processing does not occur."
  ],
  "seedData": "On store module initialization, the following demo data is inserted: Two plans — 'free' (5 clients, 10 invoices/month) and 'pro' (unlimited). One demo user: email='demo@example.com', password='demo1234', business_name='Acme Freelance Studio', default_currency='USD', assigned pro subscription. Three clients owned by the demo user: (1) 'Globex Corp', email='billing@globex.com', billing_address='100 Main St, Springfield IL 62701'; (2) 'Initech LLC', email='ap@initech.com', billing_address='4120 Freidrich Ln, Austin TX 78744'; (3) 'Umbrella Corp', email='finance@umbrella.com', billing_address='77 Arklay Dr, Raccoon City MO 65201'. Two tax rates: '0% No Tax' (rate 0, not default) and 'US Standard 10%' (rate 10, is_default=true). Five invoices across various statuses: INV-0001 (draft, Globex, 2 line items: 'Website Design' qty 1 $2500, 'Logo Package' qty 1 $800, 10% tax, no discount, due 30 days out); INV-0002 (sent, Initech, 1 line item: 'Monthly Retainer' qty 1 $3000, 10% tax, 5% discount, due 15 days out, has EmailLog entry); INV-0003 (viewed, Umbrella Corp, 3 line items: 'SEO Audit' qty 1 $1200, 'Content Writing' qty 5 $200ea, 'Analytics Setup' qty 1 $400, 10% tax, no discount, due 7 days out); INV-0004 (paid, Globex, 1 line item: 'Consulting Hours' qty 8 $150ea, 0% tax, flat $100 discount, has Payment record marked manual); INV-0005 (overdue, Initech, 1 line item: 'App Development Phase 1' qty 1 $5000, 10% tax, no discount, due date set 10 days in the past). StatusHistory records inserted for all status transitions. Dashboard summary should show ~$8,360 outstanding, ~$1,100 paid this month (INV-0004), and 1 overdue invoice."
}
```